# Multi-Reader Replication Report

Context materialization: **pass**. The frozen final_1000 baseline and selected contexts are available for 1000 examples.

Available local readers decision: **reader_available**. Runtime decision: **cpu_smoke_or_stop**.

Final placement: **limitation / appendix attempt**.

The HotpotQA v2.3 main result remains unchanged. Multi-reader outputs must not overwrite the frozen main result.

See `outputs/tables/multi_reader_replication_table.md` for the final table.
