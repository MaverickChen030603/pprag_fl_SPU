# Representative Cases DIAG

## agent_pm_bandit_slot succeeds while dynamic fails
TBD from per-query/hash evidence after DIAG runs complete.

## agent_bsp_hf_bandit succeeds while pm_bandit fails
TBD from per-query/hash evidence after DIAG runs complete.

## history state helps
TBD from per-query/hash evidence after DIAG runs complete.

## failure state helps
TBD from per-query/hash evidence after DIAG runs complete.

## rarity state neutral or misleading
TBD from per-query/hash evidence after DIAG runs complete.

## instability state conservative failure
TBD from per-query/hash evidence after DIAG runs complete.

## strict improves but QA flat
TBD from per-query/hash evidence after DIAG runs complete.

## gold_oracle_debug improves QA
TBD from per-query/hash evidence after DIAG runs complete.

## gold_oracle_debug still flat
TBD from per-query/hash evidence after DIAG runs complete.

## reader input unchanged case
TBD from per-query/hash evidence after DIAG runs complete.
